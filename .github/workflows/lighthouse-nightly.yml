name: lighthouse-nightly

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Lighthouse nightly task placeholder"
